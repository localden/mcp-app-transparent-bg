{
  "name": "mcp-transparent-widget-sample",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal MCP App sample: a transparent widget that blends with Claude's background and uses host theme tokens",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.5.0",
    "@modelcontextprotocol/sdk": "^1.28.0",
    "esbuild": "^0.25.0",
    "express": "^5.1.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^20.19.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.0"
  }
}
